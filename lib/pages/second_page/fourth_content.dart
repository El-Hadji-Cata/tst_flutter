import 'package:flutter/material.dart';

class FourthContent extends StatelessWidget {
  const FourthContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Fourth Content"),
    );
  }
}
